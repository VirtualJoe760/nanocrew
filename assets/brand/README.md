# Nano Crew brand assets — drop zone

Drop the new brand files here so I can wire them into the app icon, in-app logo, the web
nav/favicon, and the redesign. **Clean exports beat crops of the asset sheet** — please export the
individual assets from the design tool (Figma/etc.) rather than relying on the composite sheet.

## Need (in priority order)

1. **`app-icon-1024.png`** — the NC app icon, **1024×1024, no transparency, square** (the black
   rounded-square NC from the sheet, but as a flat square — the OS rounds the corners). App Store +
   Play Store both require this exact size. This is the single most important one.
2. **`nc-mark.svg`** (or `nc-mark.png`, transparent, ≥512px) — the approved NC monogram, for the
   in-app header/paywall and the nanocrew.app nav.
3. **`nc-mark-dark.png` / `nc-mark-light.png`** — white-on-transparent and black-on-transparent
   versions, if the mark needs to sit on both light and dark chrome.
4. **`favicon.png`** — 512×512 (or the SVG), for nanocrew.app.
5. **`gradient.png`** + **`symbol.svg`** — optional, for accents.

If clean exports aren't handy, drop the whole sheet as **`asset-sheet.png`** and tell me — I'll crop
the pieces out of it (raster, lower-res) as a stopgap, but please still get me a real 1024 app icon.

(General Sans is already bundled as the app font, so typography is covered.)
