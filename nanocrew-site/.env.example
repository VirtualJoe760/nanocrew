# Canonical URL of the deployed site (for OG/metadata). Optional.
SITE_URL=https://nanocrew.app

# Optional: where to persist waitlist signups. Without it, signups are accepted
# and logged but not stored. Point at the shared Supabase Postgres to persist
# into a standalone `waitlist` table.
DATABASE_URL=
